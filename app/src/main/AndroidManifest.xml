<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jbm.example.proyecto1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".accion"></activity>
        <activity android:name=".creador" />
        <activity android:name=".MisPreferidas" />
        <activity android:name=".documentales" />
        <activity android:name=".comedia" />
        <activity android:name=".terror" />
        <activity android:name=".ParaNinos" />
        <activity android:name=".MasVistos" />
        <activity android:name=".drama" />
        <activity android:name=".nuevo" />
        <activity android:name=".Pantalla1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>